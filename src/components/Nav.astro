---
const links = [
  { href: "/", label: "Home" },
  { href: "/tools", label: "Tools" }
];
---

<nav class="nav">
  {links.map((link) => (
    <a href={link.href} class="nav-link">
      {link.label}
    </a>
  ))}
</nav>
