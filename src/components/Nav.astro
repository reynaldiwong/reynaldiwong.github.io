---
const links = [
  { href: "/", label: "Home" },
  { href: "/about", label: "About" },
  { href: "/tools", label: "Tools" }
];
---

<nav class="nav">
  {links.map((link) => (
    <a href={link.href} class="nav-link">
      {link.label}
    </a>
  ))}
</nav>
